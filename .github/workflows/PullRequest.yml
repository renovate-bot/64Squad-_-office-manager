name: pull requests

on:
  push:
  pull_request_target:
    types: [opened, synchronize]

permissions:
  pull-requests: write

jobs:
  uneditable:
    uses: 64Squad/work4/.github/workflows/DevCyclePR.yml@main
